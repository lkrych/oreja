%p#notice= notice
%h1 Providers
%table
  %thead
    %tr
      %th Name
      %th Description
      %th{:colspan => "3"}
  %tbody
    - @providers.each do |provider|
      %tr
        %td= link_to "#{provider.name}", provider_path(:id => provider.id, :name => provider.name)
        %td= provider.description
        %td= link_to 'Show', provider_path(:id => provider.id, :name => provider.name)
        %td= link_to 'Edit', edit_provider_path(provider)
        %td= link_to 'Destroy', provider, method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New Provider', new_provider_path